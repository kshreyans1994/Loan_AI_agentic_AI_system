apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml
  # secret.template.yaml is intentionally NOT listed here — see its
  # header comment. Overlays / the GitOps repo supply the real Secret
  # object through Sealed Secrets / External Secrets / SOPS instead.

commonLabels:
  app.kubernetes.io/name: loaniq
  app.kubernetes.io/part-of: loan-ai-agent
